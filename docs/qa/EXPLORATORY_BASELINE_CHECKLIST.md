# چک‌لیست تست اکتشافی بیس‌لاین (نسخه فعلی)

> Task ID: P00-QA-MANUAL-018
> هدف: ثبت ایرادات آشکار نسخه فعلی پیش از شروع اصلاحات فاز ۱.

لطفاً موارد زیر را در محیط محلی خود تست کرده و ایرادات (Bugs) را در بخش "یافته‌ها" ثبت کنید.

## ۱. پوسته و افزونه وردپرس (WordPress)
- [x] نصب و فعال‌سازی افزونه `carmilla-bridge`.
- [x] نصب و فعال‌سازی پوسته `carmilla-theme`.
- [x] بررسی صفحه اصلی سایت (آیا المان‌ها به درستی نمایش داده می‌شوند؟).
- [x] افزودن یک محصول به سبد خرید و رفتن به صفحه پرداخت.
- [x] **یافته**: در Carmilla Theme،برگه ساخته‌شده با Elementor در بعضی Page Layoutها مانند `Default`،`Elementor Canvas` یا `Elementor Full Width` محتوا یا wrapper/header/footer مورد انتظار را درست نمایش نمی‌دهد. root cause باید در Template Hierarchy برگه بازتولید شود؛نبود `page.php`/`singular.php` و fallback احتمالی به `index.php` یک فرض بررسی‌نشده است. پیگیری در `P04-WPTHEME-CODE-025` انجام می‌شود و قبل از `P04-WPTHEME-GATE-036` باید بسته شود؛این مورد متعلق به `P04-WPPLUGIN-GATE-024` نیست.

## ۲. اپلیکیشن اندروید (Android)
- [x] اجرای اپلیکیشن روی امولاتور یا گوشی واقعی.
- [x] بررسی لاگین (OTP) و ورود به پنل کاربری.
- [x] مشاهده لیست محصولات و جزئیات یک محصول.
- [x] **یافته‌ها**: 

## ۳. نسخه وب / PWA
- [x] اجرای نسخه وب (`./gradlew.bat :composeApp:jsBrowserRun`).
- [x] بررسی ریسپانسیو بودن (آیا در ابعاد موبایل درست دیده می‌شود؟).
- [x] **یافته‌ها**: 

## ۴. موارد کلی و بصری
- [x] آیا متنی وجود دارد که ترجمه نشده باشد (انگلیسی مانده باشد)؟خیر
- [x] آیا آیکون یا تصویری شکسته (Broken) است؟ خیر
- [x] آیا کرش (Crash) ناگهانی در جریان‌های اصلی رخ می‌دهد؟ خیر

---

## گزارش نهایی برای تسک
- **تستر**: پوریاکاظمیه
- **تاریخ**: 2 شهریور 1405
- **نسخه (Commit Hash)**: 659c711
- **نتیجه**: [PASS ]
- **شواهد**: -
