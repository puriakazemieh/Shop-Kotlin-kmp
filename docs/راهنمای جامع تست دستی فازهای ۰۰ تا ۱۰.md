# راهنمای جامع بررسی و تست دستی Carmilla — فازهای ۰۰ تا ۱۰

> تاریخ تهیه: ۱۴۰۵/۰۶/۱۷ — ۲۰۲۶/۰۹/۰۸  
> دامنه: مخزن `kmp-shop`، محصولات WordPress، کلاینت KMP، Web/PWA و کنترل‌های تجاری تا پایان فاز ۱۰  
> وضعیت سند: چک‌لیست اجرایی زنده برای مالک محصول و تستر انسانی  
> منابع: `docs/tasks.md`، کارت‌های هر Task، `docs/MASTER_IMPLEMENTATION_CHECKLIST_FA.md`، `docs/INDEPENDENT_PRODUCTS_SPEC_FA.md`، سورس فعلی پروژه و یادداشت «موارد بررسی شاپ»

---

## ۱. این سند را چگونه استفاده کنم؟

- `[x]` یعنی وجود کد، سند یا نتیجه محدود توسط ممیزی فعلی مشاهده شده است؛ لزوماً به معنی آماده انتشار بودن کل فاز نیست.
- `[ ]` یعنی باید شما اجرا، مشاهده یا تأیید کنید.
- `⛔` یعنی قبل از اجرای تست دستی یک نقص فنی یا کمبود Evidence باید برطرف شود.
- `⚠️` یعنی نتیجه فعلی محدود، مشروط یا دارای تناقض سندی است.
- هر تست را فقط وقتی تیک بزنید که نتیجه مورد انتظار را دیده و Evidence آن را ذخیره کرده باشید.
- اگر نتیجه شکست خورد، checkbox را خالی بگذارید و شناسه نقص، توضیح و مسیر Evidence را ثبت کنید.
- هیچ داده، شماره موبایل، کلید API، رمز، اطلاعات بیمار یا مشتری واقعی را در Evidence قرار ندهید.
- وضعیت Gate یا Task را تنها پس از تکمیل همه تست‌های اجباری همان فاز و ثبت تأیید انسانی تغییر دهید.

### مشخصات اجرای فعلی

- نام تستر: `........................................`
- تاریخ شروع: `........................................`
- Commit/Build fingerprint: `........................................`
- نام محیط: `Local / Staging / RC / ................................`
- URL وردپرس تستی: `........................................`
- نسخه WordPress/PHP/WooCommerce: `........................................`
- دستگاه‌ها و مرورگرها: `........................................`
- مسیر پوشه Evidence این نوبت: `........................................`

### قالب ثبت نتیجه هر نقص

- شناسه نقص: `BUG-........`
- فاز و شماره تست: `P.. / T..`
- شدت: `Sev0 / Sev1 / Sev2 / Sev3`
- نتیجه واقعی: `........................................`
- نتیجه مورد انتظار: `........................................`
- مراحل بازتولید: `........................................`
- Screenshot/Video/Log پاک‌سازی‌شده: `........................................`
- وضعیت: `Open / Fixed / Retested / Accepted Risk`

---

## ۲. داشبورد وضعیت فعلی

| فاز | وضعیت ثبت‌شده در صف | نتیجه ممیزی فعلی | تصمیم فعلی |
|---|---|---|---|
| P00 | ۱۹ Task، همگی `DONE` | اسناد پایه و تأیید قبلی وجود دارد | قابل استفاده به‌عنوان baseline؛ اطلاعات محیط و بازار دوباره کنترل شود |
| P01 | ۳۴ Task، همگی `DONE` | کارت Gate در Completion Record برابر `BLOCKED` و QA دستی بدون مشخصات تستر است | Gate امنیت نیازمند بازبینی مجدد |
| P02 | ۲۲ Task، همگی `DONE` | Foundation و build موجود؛ Master فقط `CONDITIONAL_PASS` است | کنترل‌های deferred باید در P09 دوباره پاس شوند |
| P03 | ۳۹ Task در صف `DONE` | کارت `P03-QA-MANUAL-027` و Gate هنوز `TODO` هستند | Gate Manifest معتبر نیست تا QA جایگزین اجرا شود |
| P04 | ۵۶ Task در صف `DONE` | کارت‌های Manual/Gate هنوز `TODO`؛ Theme فعلی syntax error دارد | تست مستقل Theme/Plugin فعلاً مسدود است |
| P05 | ۲۷ Task در صف `DONE` | کارت‌های Manual و Gateها `TODO`؛ فقط مسیر ZarinPal قابل مشاهده است | Core/ZarinPal و هر provider باید مستقل تأیید شوند |
| P06 | ۱۶ Task، همگی `AWAITING_MANUAL_QA` | تنظیمات، capability، secret و queue نقص دارند | قبل از QA نیازمند اصلاح کد |
| P07 | ۲ `DONE` و ۲۵ منتظر QA | فایل‌های اصلی Migration از نظر نحوی خراب‌اند | فاز مسدود است |
| P08 | ۲۱ `DONE` و ۳ منتظر QA | PWA build می‌شود، ولی binding، cache، push و تست‌ها production-ready نیست | Gate نباید تأیید شود |
| P09 | ۷ `DONE` و ۱۲ منتظر QA | Evidence رگرسیون، امنیت و Observability کافی نیست | اجرای واقعی matrix و beta لازم است |
| P10 | ۷ `DONE` و ۱۲ منتظر QA | tag، ZIP نسخه ۱، checksum و SBOM واقعی وجود ندارند | انتشار پایدار انجام نشده است |

### توقف‌گاه‌های بحرانی قبل از شروع QA نهایی

- [ ] ⛔ خطای نحوی `wordpress/carmilla-theme/functions.php` در خطوط bootstrap برطرف و با `php -l` تأیید شده است.
- [ ] ⛔ فایل‌های Migration فاز ۰۷ بازسازی و تمام فایل‌ها با `php -l` و smoke test واقعی تأیید شده‌اند.
- [ ] ⛔ ZIPهای Theme و Bridge دوباره ساخته شده‌اند و Shared Core داخل هر دو ZIP وجود دارد.
- [ ] ⛔ تناقض وضعیت بین `docs/tasks.md`، خود کارت‌ها و Master Checklist رفع شده است.
- [ ] ⛔ تنظیمات و capability پنل پیام‌رسانی P06 اصلاح شده است.
- [ ] ⛔ تنظیم Backend و cache/security در PWA اصلاح شده و Playwright واقعی پاس شده است.
- [ ] ⛔ رگرسیون، pentest و telemetry واقعی P09 Evidence قابل بازبینی دارند.
- [ ] ⛔ Gateهای P03 تا P10 به ترتیب dependency و با تأیید انسانی ارزیابی شده‌اند.
- [ ] ⛔ قبل از انتشار P10، tag و artifactهای واقعی نسخه نهایی تولید شده‌اند.

تا وقتی موارد بالا حل نشده‌اند، هیچ ادعای «Production Ready» یا «انتشار `v1.0.0`» نباید تأیید شود.

---

## ۳. آماده‌سازی محیط و Evidence

### ۳.۱ ابزارهای لازم

- [ ] Java/JDK سازگار نصب است و `java -version` بدون خطا اجرا می‌شود.
- [ ] Android SDK و یک Emulator با API 24 و یک Emulator/دستگاه جدیدتر آماده است.
- [ ] Node.js و npm نصب‌اند.
- [ ] مرورگرهای Playwright نصب شده‌اند: `npx playwright install`.
- [ ] Docker Desktop روشن است و `docker info` موفق است.
- [ ] Git Bash یا WSL برای اجرای اسکریپت‌های ZIP موجود است.
- [ ] یک محیط WordPress تمیز و یک محیط Staging مجزا دارید.
- [ ] برای پرداخت، پیامک و ایمیل فقط sandbox/test credential دارید.

### ۳.۲ داده‌های تست

- [ ] حساب‌های مصنوعی Guest، Customer، Shop Manager و Administrator ساخته شده‌اند.
- [ ] یک محصول ساده، یک محصول متغیر، یک محصول دیجیتال و یک محصول ناموجود ساخته شده است.
- [ ] یک کد تخفیف مصنوعی و چند آدرس ساختگی آماده است.
- [ ] داده‌های clinic/psych کاملاً مصنوعی‌اند و به شخص واقعی تعلق ندارند.
- [ ] قبل از تست Migration و Upgrade از DB و `wp-content` نسخه پشتیبان گرفته شده است.

### ۳.۳ ثبت baseline مخزن

```powershell
git status --short
git log -1 --oneline
git tag --list
```

- [ ] خروجی سه فرمان بالا در Evidence ذخیره شده است.
- [ ] تغییرات موجود کاربر شناسایی شده و هیچ‌کدام پاک یا overwrite نشده‌اند.
- [ ] Commit و timestamp همه artifactهای مورد تست ثبت شده است.

---

## ۴. مرحله Build و کنترل خودکار مشترک

### ۴.۱ کامپایل KMP

```powershell
.\gradlew.bat :composeApp:compileKotlinJs :composeApp:compileKotlinJvm
```

- [x] این فرمان در ممیزی ۲۰۲۶/۰۹/۰۸ با `BUILD SUCCESSFUL` اجرا شد.
- [ ] فرمان را روی Commit مورد تحویل خودتان دوباره اجرا و log کامل را ذخیره کنید.

### ۴.۲ ساخت Android Debug برای smoke test

```powershell
.\gradlew.bat :androidApp:assembleDebug
```

- [ ] build با exit code صفر تمام می‌شود.
- [ ] APK خروجی پیدا و نام، اندازه و SHA256 آن ثبت می‌شود.
- [ ] APK روی دستگاه API 24 و دستگاه جدید نصب و اجرا می‌شود.

### ۴.۳ ساخت نسخه Web/PWA

```powershell
.\gradlew.bat :composeApp:jsBrowserDistribution
```

- [ ] خروجی production وب ساخته می‌شود.
- [ ] مسیر واقعی خروجی با runbook تطبیق داده می‌شود.
- [ ] فایل‌های `index.html`، JS bundle، manifest، Service Worker، icon و app-config موجودند.

### ۴.۴ lint همه فایل‌های PHP با Docker

```powershell
docker run --rm -v "${PWD}:/app" -w /app php:8.1-cli sh -lc "find wordpress -name '*.php' -print0 | xargs -0 -n1 php -l"
```

- [ ] تمام فایل‌های PHP عبارت `No syntax errors detected` دارند.
- [ ] هر خطای syntax قبل از ساخت ZIP اصلاح شده است.
- [ ] مشخصاً Theme bootstrap و پوشه `includes/Migration` بدون خطا هستند.

### ۴.۵ ساخت ZIPهای WordPress

```powershell
bash wordpress/build-theme-zip.sh
bash wordpress/build-bridge-zip.sh
```

- [ ] هر دو فرمان exit code صفر دارند.
- [ ] `wordpress/carmilla-theme.zip` و `wordpress/carmilla-bridge.zip` تازه تولید شده‌اند.
- [ ] داخل هر دو ZIP فایل `packages/carmilla-core/Carmilla_Kernel.php` وجود دارد.
- [ ] فایل توسعه، log، secret، test data واقعی و `.git` داخل ZIP نیست.
- [ ] SHA256 هر دو ZIP ثبت شده است.

### ۴.۶ محیط Docker وردپرس

```powershell
docker compose -f tools/test-env/docker-compose.yml down -v
docker compose -f tools/test-env/docker-compose.yml up -d
```

- [ ] WordPress و MariaDB بدون crash بالا می‌آیند.
- [ ] log کانتینرها خطای Fatal PHP یا loop مهاجرت ندارد.
- [ ] قبل از هر matrix مهم، محیط با volume تمیز دوباره ساخته می‌شود.

### ۴.۷ Playwright PWA

```powershell
npx playwright install
npm run test:e2e:fake
```

- [x] تولید فایل PWA در ممیزی فعلی موفق شد.
- [ ] هر ۲۸ تست واقعاً اجرا شوند و صرفاً به علت نبود browser skip/fail نشوند.
- [ ] assertionهای placeholder مانند `expect(true)` با آزمون رفتار واقعی جایگزین شده باشند.
- [ ] نتیجه fixture به‌عنوان نتیجه production ثبت نشود.

---

# ۵. چک‌لیست فاز به فاز

## فاز ۰۰ — Baseline، Scope، QA و Business Foundation

### آنچه موجود یا انجام‌شده است

- [x] صف پروژه هر ۱۹ Task فاز صفر را `DONE` نشان می‌دهد.
- [x] Test Strategy، Test Data، Reference Environments و exploratory checklist وجود دارند.
- [x] در Master Checklist، Gate فاز صفر `PASS` ثبت شده است.
- [x] ثبت قبلی تستر برای `P00-QA-MANUAL-018` وجود دارد.

### بازبینی دستی لازم

- [ ] `P00-T01` دامنه نسخه مورد تست را بنویسید: SKU، میزبان WordPress، Backend، target و قابلیت‌های داخل build.
- [ ] `P00-T02` inventory شناسه‌ها را با سورس فعلی تطبیق دهید: applicationId، package، domain، signing owner و artifactهای قبلی.
- [ ] `P00-T03` تأیید کنید فقط دو Backend Profile رسمی `WORDPRESS` و `SPRING` وجود دارد.
- [ ] `P00-T04` محیط‌های F0 تا F4 را مرور و برای این نوبت محیط‌های قابل اجرا را مشخص کنید.
- [ ] `P00-T05` روی Android، Web و Desktop یک exploratory smoke اجرا و screenshot/video ثبت کنید.
- [ ] `P00-T06` اگر macOS/iOS در دسترس نیست، iOS را صریحاً `BLOCKED/DEFERRED` ثبت کنید؛ آن را PASS نزنید.
- [ ] `P00-T07` نسخه‌های حداقل WordPress/PHP/WooCommerce با قرارداد فروش فعلی تطبیق دارند.
- [ ] `P00-T08` توافق‌های بازار، پشتیبانی، refund و design partner تاریخ‌دار و هنوز معتبرند.
- [ ] `P00-T09` هیچ secret یا داده واقعی در fixture، log و Evidence نیست.

### معیار عبور فاز صفر

- [ ] Scope نسخه، مالک هر artifact و محیط QA بدون ابهام ثبت شده است.
- [ ] baseline روی Commit فعلی قابل تکرار است.
- [ ] هر blocker شناسه، owner و مقصد backlog دارد.
- [ ] Gate توسط مالک محصول با تاریخ و build fingerprint تأیید شده است.

---

## فاز ۰۱ — Stop-Ship Security و صحت تراکنش

### آنچه موجود یا انجام‌شده است

- [x] کد redaction و debug logging محدود در KMP وجود دارد.
- [x] JWT HMAC، ownership checks و تعدادی feature fail-closed در WordPress دیده می‌شود.
- [x] صف، همه ۳۴ Task را `DONE` نشان می‌دهد.

### تناقض و توقف‌گاه

- [ ] ⛔ Completion Record کارت `P01-SECURITY-GATE-023` هنوز `BLOCKED` است.
- [ ] ⛔ کارت `P01-QA-MANUAL-021` مشخصات Manual tester/date/result کامل ندارد.
- [ ] ⚠️ Master Checklist تصمیم Gate فاز ۱ را `NOT_EVALUATED` نشان می‌دهد.

### تست دستی امنیت

- [ ] `P01-T01` با دو Customer مصنوعی وارد شوید؛ با token کاربر A سفارش، آدرس، فایل یا رکورد کاربر B را بخوانید/ویرایش/حذف کنید. همه درخواست‌ها باید `403/404` شوند.
- [ ] `P01-T02` OTP اشتباه را تا سقف attempts بزنید؛ پس از سقف باید قفل شود.
- [ ] `P01-T03` OTP منقضی، مصرف‌شده و متعلق به purpose دیگر باید رد شود.
- [ ] `P01-T04` ارسال مجدد OTP قبل از cooldown باید رد یا rate-limit شود.
- [ ] `P01-T05` token منقضی، دستکاری‌شده، با issuer/audience اشتباه و revoked باید رد شود.
- [ ] `P01-T06` با حساب Customer و Shop Manager به صفحات و endpointهای تنظیمات کلان، سلامت، secret و نقش‌ها درخواست دهید؛ دسترسی باید رد شود.
- [ ] `P01-T07` یک origin غیرمجاز برای CORS ارسال کنید؛ پاسخ production نباید `Access-Control-Allow-Origin: *` یا origin مهاجم را برگرداند.
- [ ] `P01-T08` API host را به host غیرمجاز تغییر دهید؛ Bearer token نباید برای آن host ارسال شود.
- [ ] `P01-T09` logهای Android، Browser، WordPress و proxy را بررسی کنید؛ token، Authorization، password، OTP کامل، موبایل کامل و payload حساس نباید دیده شود.
- [ ] `P01-T10` deep link پرداخت جعلی با `status=success` باز کنید؛ کلاینت باید status را از سرور verify کند و صرفاً query را نپذیرد.
- [ ] `P01-T11` پرداخت ناموفق/لغوشده نباید cart را پاک یا order را paid کند.
- [ ] `P01-T12` Wallet، Booking، LMS و Clinic خارج از SKU را از UI، deep link و API مستقیم امتحان کنید؛ باید fail-closed باشند.
- [ ] `P01-T13` اگر Spring در دامنه تحویل نیست، exposure آن را بسته/allowlist کنید؛ اگر در دامنه است، همین matrix امنیت روی Spring هم اجرا شود.

### معیار عبور فاز ۰۱

- [ ] هیچ P0/Sev0/Sev1 امنیتی باز نیست.
- [ ] Evidence شامل request ID پاک‌سازی‌شده، tester، تاریخ، محیط و build است.
- [ ] Gate card، queue و Master یک وضعیت واحد دارند.

---

## فاز ۰۲ — Engineering Foundation و Quality Harness

### آنچه موجود یا انجام‌شده است

- [x] `build-logic`، conventionها، ماژول `androidApp` و تست‌های پایه در مخزن وجود دارند.
- [x] کامپایل KMP برای JS/JVM در ممیزی فعلی موفق است.
- [x] صف، همه ۲۲ Task را `DONE` نشان می‌دهد.
- [x] Master این فاز را `CONDITIONAL_PASS` و کنترل‌های باقی‌مانده را به P09 منتقل کرده است.

### تست دستی و عملیاتی

- [ ] `P02-T01` build از checkout تمیز و بدون cache قبلی اجرا و موفق شود.
- [ ] `P02-T02` تمام unit testهای مرتبط KMP، Android و WordPress harness اجرا و report ذخیره شود.
- [ ] `P02-T03` محیط integration وردپرس با PHP/WP/Woo matrix بالا بیاید؛ صرف compile جای integration test را نگیرد.
- [ ] `P02-T04` test profile سرور، در صورت ادعای پشتیبانی Spring، بدون وابستگی به DB توسعه‌دهنده اجرا شود.
- [ ] `P02-T05` CI روی یک PR آزمایشی lint، test، package و secret scan را اجرا کند و failure را واقعاً block کند.
- [ ] `P02-T06` dependency verification/locking روی checkout جدید قابل تکرار باشد.
- [ ] `P02-T07` APK نصب شود و رفتار اصلی قبل/بعد Foundation تفاوت ناخواسته نداشته باشد.
- [ ] `P02-T08` rollback/forward-fix نسخه Foundation روی DB و artifact قبلی تمرین شود.
- [ ] `P02-T09` exceptionهای معماری با owner و deadline ثبت شوند؛ به‌خصوص موارد deferred تا P09.

### معیار عبور فاز ۰۲

- [ ] همه کنترل‌های deferred ذکرشده در کارت P02 Gate داخل Evidence فاز ۰۹ دوباره تأیید شده‌اند.
- [ ] build فقط روی ماشین توسعه‌دهنده موفق نیست و در CI قابل تکرار است.

---

## فاز ۰۳ — BuildSpec، دو Backend Profile و Feature Manifest

### آنچه موجود یا انجام‌شده است

- [x] ماژول capability/config، مدل BuildSpec، resolver و تست‌های مربوط در مخزن وجود دارند.
- [x] صف، ۳۹ Task را `DONE` نشان می‌دهد.

### تناقض و توقف‌گاه

- [ ] ⛔ کارت جایگزین `P03-QA-MANUAL-027` هنوز `TODO` است.
- [ ] ⛔ کارت `P03-MANIFEST-GATE-022` هنوز `TODO` است.
- [ ] ⛔ QA قبلی P03 در Completion Record گفته محیط live و build deploy‌شده در دسترس نبوده، ولی در صف `DONE` شده است.
- [ ] ⚠️ Master Gate را `NOT_EVALUATED` نشان می‌دهد.

### تست دستی Manifest و Feature Guard

- [ ] `P03-T01` یک build مشخص WORDPRESS و یک fixture/build مشخص SPRING بسازید؛ هیچ Backend سوم یا fallback مبهم وجود نداشته باشد.
- [ ] `P03-T02` BuildSpec را دوبار با ورودی یکسان بسازید؛ خروجی و hash باید deterministic باشد.
- [ ] `P03-T03` در wp-admin یک قابلیت مجاز را خاموش کنید؛ بدون rebuild، منو و صفحه آن در کلاینت حذف یا غیرفعال شود.
- [ ] `P03-T04` deep link همان قابلیت خاموش را باز کنید؛ کاربر باید به صفحه امن/عدم دسترسی برود.
- [ ] `P03-T05` API مستقیم قابلیت خاموش را بزنید؛ backend باید `403/404` بدهد و فقط UI پنهان نشده باشد.
- [ ] `P03-T06` قابلیت dependencyدار را بدون پیش‌نیاز فعال کنید؛ validation باید فعال‌سازی ناسازگار را رد کند.
- [ ] `P03-T07` manifest خراب، امضای نامعتبر، نسخه آینده یا capability ناشناخته ارسال کنید؛ سیستم باید fail-closed شود.
- [ ] `P03-T08` شبکه manifest را قطع کنید؛ فقط last-known-good مجاز و تاریخ‌دار استفاده شود و bypass رخ ندهد.
- [ ] `P03-T09` app را Force Stop/Restart کنید؛ وضعیت مؤثر قابلیت‌ها باید صحیح باقی بماند.
- [ ] `P03-T10` tenant/origin را تغییر دهید؛ token، cache و manifest tenant قبلی نباید قابل استفاده باشد.
- [ ] `P03-T11` حالت‌های Theme-only، Plugin-only و co-install همان Feature Manifest را تولید و enforce کنند.
- [ ] `P03-T12` legacy flavor/package/token migration بدون logout ناخواسته یا برخورد namespace اجرا شود.

### معیار عبور فاز ۰۳

- [ ] UI، route، deep link، use-case، network و backend همگی از یک وضعیت مؤثر پیروی می‌کنند.
- [ ] هیچ مسیر مستقیم برای دورزدن feature-off وجود ندارد.
- [ ] QA027 و Gate022 با tester/date/build/result واقعی تکمیل شده‌اند.

---

## فاز ۰۴ — Theme و Plugin مستقل و Co-install

### آنچه موجود یا انجام‌شده است

- [x] Shared Core در سورس و اسکریپت‌های ساخت دو ZIP وجود دارد.
- [x] Theme، Bridge، REST controllerها، Woo adapter، capability UI و اسناد اولیه وجود دارند.
- [x] صف، ۵۶ Task را `DONE` نشان می‌دهد.

### تناقض و توقف‌گاه

- [ ] ⛔ Theme فعلی در bootstrap عبارت نامعتبر `\ =` دارد و قبل از QA باید اصلاح شود.
- [ ] ⛔ ZIPهای موجود تاریخ قدیمی دارند و در بررسی فعلی Shared Core داخل آن‌ها پیدا نشد؛ باید rebuild شوند.
- [ ] ⛔ کارت‌های `P04-QA-MANUAL-021`، `P04-WPPLUGIN-MANUAL-034`، `P04-WORDPRESS-MANUAL-035` و `P04-QA-MANUAL-022` هنوز `TODO` هستند.
- [ ] ⛔ سه کارت Gate P04 هنوز `TODO` و Master برابر `NOT_EVALUATED` است.

### Matrix نصب اجباری

- [ ] `P04-T01` فقط `carmilla-theme.zip` را روی WordPress تمیز نصب و فعال کنید؛ Fatal Error یا نیاز اجباری به Bridge نباشد.
- [ ] `P04-T02` فقط `carmilla-bridge.zip` را با Twenty Twenty-Four نصب کنید؛ API و UI عمومی/مدیریتی لازم بدون Carmilla Theme کار کند.
- [ ] `P04-T03` Bridge را با Storefront تست کنید.
- [ ] `P04-T04` Bridge را با حداقل یک قالب ثالث منتخب بازار تست کنید.
- [ ] `P04-T05` ابتدا Theme و سپس Bridge را نصب کنید؛ فقط یک Shared Kernel boot شود.
- [ ] `P04-T06` ابتدا Bridge و سپس Theme را نصب کنید؛ نتیجه با ترتیب قبل یکسان باشد.
- [ ] `P04-T07` هر دو ترتیب upgrade را اجرا کنید؛ duplicate class/table/hook یا data loss رخ ندهد.
- [ ] `P04-T08` یکی از دو میزبان را غیرفعال کنید؛ میزبان باقی‌مانده و داده مشترک سالم بماند.

### قابلیت‌ها، SKU و دسترسی

- [ ] `P04-T09` صفحه Carmilla Capabilities باز و وضعیت purchased/enabled/prerequisite/release-ready را متمایز نشان دهد.
- [ ] `P04-T10` WooCommerce را خاموش کنید؛ قابلیت فروشگاه با پیام قابل فهم fail-closed شود و سایت نشکند.
- [ ] `P04-T11` قابلیت خریداری‌نشده را از UI، REST مستقیم و App Builder درخواست کنید؛ همه باید رد شوند.
- [ ] `P04-T12` قابلیت خریداری‌شده ولی خاموش را تست کنید؛ داده حفظ شود ولی عملیات و UI غیرفعال باشند.
- [ ] `P04-T13` Customer/Author نباید منوی مدیریت، secret، entitlement یا App Builder را ببیند.
- [ ] `P04-T14` درخواست یک سایت/tenant دیگر با license یا idempotency key این سایت رد شود.

### فروشگاه و سازگاری WooCommerce

- [ ] `P04-T15` محصولات ساده، متغیر، دیجیتال، تخفیف‌خورده و ناموجود در Theme-only نمایش صحیح دارند.
- [ ] `P04-T16` همان محصولات در Plugin-only با قالب ثالث نمایش و API صحیح دارند.
- [ ] `P04-T17` Cart، Checkout و Order با HPOS روشن کار می‌کنند.
- [ ] `P04-T18` Cart/Checkout Blocks روشن شوند و checkout بدون ناسازگاری کار کند.
- [ ] `P04-T19` یک سفارش ساخته، مشاهده، لغو و دوباره سفارش داده شود؛ ownership رعایت شود.
- [ ] `P04-T20` privacy export/erase روی داده مصنوعی اجرا و retention policy بررسی شود.

### UI، RTL و Accessibility

- [ ] `P04-T21` صفحات Home، Blog، Product، Cart، Checkout، Account و خطا در RTL و LTR بررسی شوند.
- [ ] `P04-T22` Dark Mode، موبایل، تبلت و دسکتاپ بدون بریدگی یا scroll افقی ناخواسته باشند.
- [ ] `P04-T23` keyboard navigation، focus، label فرم و zoom 200٪ قابل استفاده باشد.
- [ ] `P04-T24` Elementor Canvas/Full Width، در صورت ادعای پشتیبانی، با layout واقعی تست شود.
- [ ] `P04-T25` Empty/Error/Offline state پیام قابل فهم و بدون دکمه ماژول حذف‌شده داشته باشد.

### App Builder در مرز P04

- [ ] `P04-T26` دو درخواست build با idempotency key یکسان فقط یک job/write بسازند.
- [ ] `P04-T27` target خریداری‌نشده رد شود.
- [ ] `P04-T28` خروجی fake runner صریحاً Experimental/Mock باشد و artifact قابل فروش معرفی نشود.

### معیار عبور فاز ۰۴

- [ ] هر سه حالت Theme-only، Plugin-only و co-install با ZIP و checksum مشخص تست شده‌اند.
- [ ] استقلال محصولات و نبود duplicate/data loss اثبات شده است.
- [ ] Gate Theme، Gate Plugin و Gate co-install جداگانه توسط انسان تأیید شده‌اند.

---

## فاز ۰۵ — Payment Platform، ZarinPal، BNPL و PSP مستقیم

### آنچه موجود یا انجام‌شده است

- [x] مسیر request/verify زرین‌پال در Bridge وجود دارد.
- [x] ownership سفارش، تطبیق Authority و کنترل amount در این مسیر دیده می‌شود.
- [x] صف، ۲۷ Task را `DONE` نشان می‌دهد.

### تناقض و توقف‌گاه

- [ ] ⛔ کارت‌های Manual و هر چهار Gate P05 هنوز `TODO` هستند و Master `NOT_EVALUATED` است.
- [ ] ⛔ پیاده‌سازی قابل مشاهده زرین‌پال Bridge-specific است؛ parity در Theme-only باید اثبات یا اصلاح شود.
- [ ] ⛔ کد عملیاتی DigiPay، SnappPay و PSP مستقیم در مسیرهای بررسی‌شده پیدا نشد؛ بدون contract و sandbox نباید DONE یا advertised باشند.

### تنظیمات و امنیت پرداخت

- [ ] `P05-T01` Merchant ID و Deep Link فقط با sandbox تنظیم شوند و در UI/log/REST/export افشا نشوند.
- [ ] `P05-T02` Customer نتواند order متعلق به کاربر دیگر را برای پرداخت درخواست کند.
- [ ] `P05-T03` amount، currency، order ID، Authority/reference و provider با داده ذخیره‌شده تطبیق داده شوند.
- [ ] `P05-T04` تغییر مبلغ سفارش بین request و verify باید پرداخت را fail کند.
- [ ] `P05-T05` callback تکراری و verify مجدد نباید دوبار fulfillment، ledger یا stock update ایجاد کند.
- [ ] `P05-T06` callback با Authority اشتباه، order اشتباه یا status جعلی رد شود.

### جریان End-to-End زرین‌پال

- [ ] `P05-T07` در Theme-only یک سفارش مصنوعی بسازید و پرداخت موفق sandbox را کامل کنید.
- [ ] `P05-T08` در Plugin-only با قالب ثالث همین پرداخت را کامل کنید.
- [ ] `P05-T09` در co-install همین پرداخت را کامل کنید و فقط یک callback/fulfillment ثبت شود.
- [ ] `P05-T10` پس از پرداخت موفق، سفارش `Processing/Completed` و cart فقط بعد از verify سرور خالی شود.
- [ ] `P05-T11` پرداخت لغوشده/ناموفق، سفارش را paid نکند و cart را حفظ کند.
- [ ] `P05-T12` بستن WebView، قطع اینترنت و بازگشت دیرهنگام را تست کنید؛ status از سرور بازیابی شود.
- [ ] `P05-T13` deep link موفق/ناموفق روی Android و PWA صفحه درست نشان دهد ولی حقیقت نهایی را از API بگیرد.
- [ ] `P05-T14` reconciliation مبلغ سفارش با گزارش sandbox اختلاف صفر داشته باشد.
- [ ] `P05-T15` refund کامل و جزئی اجرا و وضعیت سفارش/ledger/گزارش تطبیق داده شود.
- [ ] `P05-T16` rollback نسخه پرداخت روی snapshot تمرین و بدون از دست‌رفتن سفارش انجام شود.

### Gate مستقل providerها

- [ ] `P05-T17` DigiPay فقط پس از contract، sandbox، callback، delivery، refund، settlement و kill switch PASS شود؛ در غیر این صورت `DEFERRED` بماند.
- [ ] `P05-T18` SnappPay فقط پس از merchant docs، sandbox و همان matrix PASS شود؛ در غیر این صورت `DEFERRED` بماند.
- [ ] `P05-T19` PSP مستقیم فقط برای terminal قراردادشده و با verify/refund/settlement واقعی PASS شود.
- [ ] `P05-T20` provider تأییدنشده از UI و صفحه فروش حذف یا با برچسب «پشتیبانی‌نشده» مشخص شود.

### معیار عبور فاز ۰۵

- [ ] Gate Core/ZarinPal با مبلغ، verify، replay، reconciliation، refund و rollback سبز است.
- [ ] نتیجه هر provider جدا ثبت شده و هیچ Gate با provider دیگر جایگزین نشده است.

---

## فاز ۰۶ — SMS، Email، OTP و Secret Management

### آنچه موجود است

- [x] schema تنظیمات، آداپتر HTTP پیامک، Mail adapter، Template Engine، Queue و صفحه Test در سورس وجود دارند.
- [x] دفاع اولیه HTTPS، `wp_safe_remote_request` و جلوگیری از redirect در آداپتر پیامک دیده می‌شود.
- [x] همه ۱۶ Task در صف منتظر QA هستند و Gate بسته نشده است.

### توقف‌گاه‌های فنی

- [ ] ⛔ `MessageSettings::init()` در Shared Kernel اجرا شود تا setting واقعاً register شود.
- [ ] ⛔ capability `manage_carmilla_integrations` به نقش مجاز تخصیص داده شود.
- [ ] ⛔ تب‌های SMS، Email، Templates و Health از placeholder به فرم عملیاتی تبدیل شوند.
- [ ] ⛔ مسیر Theme-only برای تنظیم و ارسال پیام مستقل از Bridge پیاده/تأیید شود.
- [ ] ⛔ رمزنگاری CBC و fallback ثابت با AEAD، fail-closed، key خارج DB و rotation/versioning جایگزین شود.
- [ ] ⛔ Queue از ذخیره PII کامل در arguments خودداری و idempotency پایدار داشته باشد.
- [ ] ⛔ آداپتر Generic HTTP امکان mapping کنترل‌شده method/header/body/response را داشته باشد.
- [ ] ⛔ smoke test PHP واقعاً اجرا شود؛ «آماده اجرا» به‌عنوان PASS پذیرفته نشود.

### تست دستی پیام‌رسانی

- [ ] `P06-T01` با Administrator صفحه `Carmilla → Integrations` را در Theme-only باز کنید.
- [ ] `P06-T02` همان صفحه را در Plugin-only با قالب ثالث باز کنید.
- [ ] `P06-T03` در co-install تنها یک تنظیم مشترک و یک منو دیده شود.
- [ ] `P06-T04` با Customer/Author صفحه و endpoint تنظیمات را باز کنید؛ دسترسی باید رد شود.
- [ ] `P06-T05` API key مصنوعی را ذخیره کنید؛ در DB plaintext نباشد و در UI دوباره نمایش داده نشود.
- [ ] `P06-T06` key را rotate کنید؛ تنظیمات قبلی طبق policy مهاجرت یا fail-closed شوند.
- [ ] `P06-T07` URLهای `http://localhost`، `127.0.0.1`، IP خصوصی، redirect و hostname تغییرکننده DNS را تست کنید؛ همگی رد شوند.
- [ ] `P06-T08` SMS و Email تستی به sandbox ارسال و provider message ID ذخیره شود.
- [ ] `P06-T09` provider خطای 400، 401، 429 و 500 بدهد؛ retry policy مطابق قرارداد باشد.
- [ ] `P06-T10` event یکسان را چندبار ثبت کنید؛ فقط یک پیام نهایی ارسال شود.
- [ ] `P06-T11` Scheduled Actions را بررسی کنید؛ recipient/message/secret کامل نباید قابل مشاهده باشد.
- [ ] `P06-T12` OTP درخواست کنید؛ قالب، زبان، RTL و expiry صحیح باشند.
- [ ] `P06-T13` template شامل HTML نامجاز، PHP/eval و placeholder ناشناخته ارسال کنید؛ sanitize یا reject شود.
- [ ] `P06-T14` debug.log، REST response، export و support bundle را جستجو کنید؛ secret و PII کامل وجود نداشته باشد.
- [ ] `P06-T15` OTP فوری و پیام غیرفوری queue شده هر دو در سه host mode رفتار صحیح داشته باشند.

### معیار عبور فاز ۰۶

- [ ] هر دو ZIP بدون محصول Carmilla دیگر قابلیت تنظیم و ارسال دارند.
- [ ] امنیت secret، SSRF، log و queue با Evidence واقعی پاس شده است.
- [ ] هزینه و محدودیت provider برای هر SKU مستند شده است.

---

## فاز ۰۷ — Seed/Import و Customer Migration

### آنچه موجود است

- [x] seed packها، Importer، schema و فایل‌های طراحی Migration در مخزن وجود دارند.
- [x] راهنمای Seed/Migration و پوشه‌های Evidence ایجاد شده‌اند.
- [x] دو Gate این فاز هنوز منتظر تأیید هستند.

### توقف‌گاه‌های بحرانی

- [ ] ⛔ همه `\ =`ها و پارامترهای خراب در فایل‌های Migration و smoke test اصلاح شده‌اند.
- [ ] ⛔ همه فایل‌های PHP با lint واقعی پاس شده‌اند.
- [ ] ⛔ feature resolver Importer به manifest/active feature واقعی متصل شده است.
- [ ] ⛔ handlerهای schemaهای shop/academy/clinic/psych وجود دارند و تست شده‌اند.
- [ ] ⛔ unique key مناسب site/pack/entity و state مجزای chunk تعریف شده است.
- [ ] ⛔ dry-run تعداد واقعی create/update/skip/conflict را گزارش می‌دهد.
- [ ] ⛔ lock اتمیک، media validation کامل و rollback قابل اتکا پیاده شده است.
- [ ] ⛔ Evidence خودکار با exit code واقعی جایگزین عبارت «exit code 0 expected» شده است.

### تست Seed/Import Core

- [ ] `P07-T01` روی WordPress تمیز، dry-run بسته base را اجرا و شمارش را ثبت کنید.
- [ ] `P07-T02` import base را اجرا؛ نوشته‌ها، صفحات و رسانه‌ها صحیح ساخته شوند.
- [ ] `P07-T03` dry-run و import بسته shop را اجرا؛ محصولات ساده/متغیر/دیجیتال/ناموجود/Sale ایجاد شوند.
- [ ] `P07-T04` import را دوباره اجرا؛ هیچ duplicate ایجاد نشود و همه آیتم‌های بدون تغییر skip شوند.
- [ ] `P07-T05` import را حدود ۳۰٪ قطع و resume کنید؛ داده ناقص یا duplicate نماند.
- [ ] `P07-T06` دو import هم‌زمان شروع کنید؛ lock باید یکی را متوقف کند.
- [ ] `P07-T07` WooCommerce را خاموش و shop seed را اجرا کنید؛ بسته باید skip/fail-safe شود و سایت نشکند.
- [ ] `P07-T08` یک محتوای seedشده را دستی ویرایش و rollback کنید؛ ویرایش کاربر حفظ و داده تغییرنکرده طبق policy حذف شود.
- [ ] `P07-T09` media با domain، MIME، size و hash نامعتبر باید رد شود.
- [ ] `P07-T10` seedهای Academy/Clinic/Psych را فقط در صورت فعال‌بودن feature مربوط تست کنید.
- [ ] `P07-T11` seedهای Clinic/Psych هیچ PII/PHI واقعی نداشته باشند.

### تست Export و Migration

> دستورات زیر فقط پس از رفع syntax و روی backup/staging اجرا شوند.

```bash
wp carmilla export-legacy --encryption_key="<32-byte-test-key>" --customer_uuid="test-site"
wp carmilla import-legacy --encryption_key="<32-byte-test-key>" --site_uuid="test-site" --consent=yes
wp carmilla export-overlay
```

- [ ] `P07-T12` export بدون consent موردنیاز یا با policy ناقص رد شود.
- [ ] `P07-T13` خروجی شامل order، payment، user credential، داده بیمار یا metaهای denylist نباشد.
- [ ] `P07-T14` خروجی رمزنگاری‌شده plaintext قابل خواندن نداشته باشد.
- [ ] `P07-T15` key اشتباه، customer UUID اشتباه، payload دستکاری‌شده و expiry گذشته همگی fail-closed شوند.
- [ ] `P07-T16` import صحیح دو pass را کامل و mapping والد/فرزند را حفظ کند.
- [ ] `P07-T17` اجرای دوم delta، همه رکوردهای بدون تغییر را skip کند.
- [ ] `P07-T18` domainهای قدیمی در content/media به domain جدید بازنویسی شوند و URL خارجی مجاز دست‌نخورده بماند.
- [ ] `P07-T19` overlay فقط تغییرات مشتری و محتوای جدید را داشته باشد.
- [ ] `P07-T20` backup restore واقعی اجرا و زمان/نتیجه ثبت شود.

### معیار عبور فاز ۰۷

- [ ] Gate A فقط برای Seed/Import base+shop و PWA با repeat/resume/rollback سبز PASS شده است.
- [ ] Gate B جداگانه برای Migration مشتری با consent/encryption/delta/UAT PASS شده است.
- [ ] Gate A و Gate B با هم یا با Gate add-onهای آینده اشتباه نشده‌اند.

---

## فاز ۰۸ — Web/PWA مستقل

### آنچه موجود است

- [x] manifestها، app-configها، Service Worker، offline page، push API اولیه و runbook وجود دارند.
- [x] `generatePwaFiles` در ممیزی فعلی موفق شد.
- [x] Manual QA مرورگر، Accessibility و Gate هنوز منتظر تأییدند.

### توقف‌گاه‌های فنی

- [ ] ⛔ profile WordPress و Spring مستقیماً از BuildSpec معتبر تولید شوند؛ localhost و SKU نامرتبط hardcode نباشد.
- [ ] ⛔ `?api=` دلخواه از production حذف یا با allowlist سخت‌گیرانه مدیریت شود.
- [ ] ⛔ Service Worker به‌جای denylist ناقص، فقط asset/public responseهای صریح را cache کند.
- [ ] ⛔ CSP بدون `unsafe-eval`/wildcard غیرضروری و با headerهای واقعی server تنظیم شود.
- [ ] ⛔ VAPID dummy حذف و subscribe/consent/revoke backend پیاده شود.
- [ ] ⛔ logout و token expiry فقط cache/token همان tenant را امن پاک کنند.
- [ ] ⛔ تست‌های placeholder اصلاح و Playwright با browser واقعی اجرا شود.
- [ ] ⛔ اختلاف وضعیت کارت `P08-PWA-CODE-006` با صف و پوشه‌های Evidence ناقص رفع شود.

### راه‌اندازی تست PWA

```powershell
.\gradlew.bat :composeApp:jsBrowserDistribution
npx serve composeApp/build/dist/js/productionExecutable -p 8080
```

- [ ] `P08-T01` مسیر واقعی distribution را بررسی و server را از همان مسیر اجرا کنید.
- [ ] `P08-T02` در DevTools → Application، manifest بدون خطا، iconها صحیح و SW `Activated` باشد.
- [ ] `P08-T03` نصب standalone روی Chrome Android موفق باشد.
- [ ] `P08-T04` نصب/Add to Home Screen روی Safari iOS طبق محدودیت پلتفرم موفق باشد.

### Cache، Offline و Tenant Isolation

- [ ] `P08-T05` فقط shell و داده عمومی مجاز در Cache Storage وجود داشته باشد.
- [ ] `P08-T06` auth، order، payment، profile، address، favorite، support، clinic و psych در cache نباشند.
- [ ] `P08-T07` در Offline، صفحه عمومی cacheشده یا offline state قابل فهم نشان داده شود؛ صفحه سفید نباشد.
- [ ] `P08-T08` عملیات write در Offline با 503/پیام درست fail شود و بعداً duplicate نشود.
- [ ] `P08-T09` Tenant A و B روی یک browser profile cache/token/manifest مشترک نداشته باشند.
- [ ] `P08-T10` logout و token expiry داده tenant جاری را پاک کنند و داده tenant دیگر را خراب نکنند.

### Update، Deep Link و Backend

- [ ] `P08-T11` نسخه جدید SW منتشر کنید؛ prompt/update flow کار کند و mismatch loop ایجاد نشود.
- [ ] `P08-T12` rollback به نسخه قبلی تمرین و cache version صحیح بازیابی شود.
- [ ] `P08-T13` deep link مانند `/products/123` را مستقیم و با refresh باز کنید؛ 404 نگیرید.
- [ ] `P08-T14` deep link محافظت‌شده برای مهمان باید به login/deny امن برود.
- [ ] `P08-T15` profile WORDPRESS به URL واقعی همان tenant وصل شود.
- [ ] `P08-T16` profile SPRING فقط اگر Gate سرور مربوط پاس است به محیط واقعی وصل شود؛ fixture production محسوب نشود.
- [ ] `P08-T17` deployment در root و subdirectory جداگانه تست شود.

### Security، Push، Performance و Accessibility

- [ ] `P08-T18` headerهای CSP، Referrer-Policy، HSTS و Permissions-Policy را در Network response بررسی کنید.
- [ ] `P08-T19` origin غیرمجاز نتواند API/token را مصرف کند.
- [ ] `P08-T20` push permission فقط پس از اقدام روشن کاربر درخواست شود.
- [ ] `P08-T21` subscription در backend ثبت، notification دریافت و revoke واقعی انجام شود.
- [ ] `P08-T22` payload نوتیفیکیشن PII/PHI یا secret نداشته باشد.
- [ ] `P08-T23` FCP/LCP روی شبکه و دستگاه ضعیف زیر بودجه ۳ ثانیه یا طبق budget مصوب باشد.
- [ ] `P08-T24` Chrome Android، Firefox، Edge و Safari iOS تست شوند.
- [ ] `P08-T25` RTL/LTR، keyboard، Screen Reader و zoom 200٪ بدون مانع بحرانی کار کنند.

### معیار عبور فاز ۰۸

- [ ] install/update/offline/security/performance/accessibility برای artifact و backend مشخص سبز است.
- [ ] Manual 016، Manual 017 و Gate 019 با Evidence انسانی تکمیل شده‌اند.

---

## فاز ۰۹ — Regression، Security، Observability و Beta

### آنچه موجود است

- [x] Regression Traceability Matrix و کارت‌های suite ایجاد شده‌اند.
- [x] کامپایل JS/JVM موفق است.
- [x] ساختار اولیه Telemetry opt-in و redaction وجود دارد.
- [x] ۱۲ Task انسانی/Gate همچنان منتظر QA هستند.

### توقف‌گاه‌های فنی و Evidence

- [ ] ⛔ تست `P09-QA-AUTO-002` از compile-only به regression واقعی shop/payment/manifest/import/PWA تبدیل شود.
- [ ] ⛔ جدول‌ها و endpointهای Traceability Matrix با سورس واقعی تطبیق داده شوند.
- [ ] ⛔ Telemetry backend، persistence رضایت، call siteها و dashboard واقعی داشته باشد.
- [ ] ⛔ گزارش pentest شامل scope،command/tool،findings،fix و retest باشد.
- [ ] ⛔ ZIP/rollback/restore/support bundle با artifact واقعی و checksum اثبات شوند.

### Regression نصب، ارتقا و بازگشت

- [ ] `P09-T01` ZIPهای RC روی WordPress تمیز نصب و فعال شوند.
- [ ] `P09-T02` داده محصول/سفارش بسازید و upgrade به RC جدید را انجام دهید؛ داده حفظ شود.
- [ ] `P09-T03` rollback/forward-fix روی snapshot اجرا شود؛ schema ناسازگار یا data loss رخ ندهد.
- [ ] `P09-T04` PHP 7.4/8.1/8.2 و نسخه‌های WordPress/WooCommerce پشتیبانی‌شده طبق matrix تست شوند.
- [ ] `P09-T05` HPOS و Checkout Blocks روشن و خاموش تست شوند.

### Regression کامل فروشگاه

- [ ] `P09-T06` ثبت‌نام، login با password و OTP و logout کامل شود.
- [ ] `P09-T07` catalog، search/filter، product detail و variantها صحیح باشند.
- [ ] `P09-T08` add/change/remove cart و coupon صحیح محاسبه شوند.
- [ ] `P09-T09` checkout مهمان/کاربر طبق قرارداد، آدرس و shipping صحیح باشد.
- [ ] `P09-T10` پرداخت موفق، ناموفق، لغوشده، timeout و callback تکراری تست شوند.
- [ ] `P09-T11` در پرداخت لغوشده یا ناموفق cart حفظ شود.
- [ ] `P09-T12` سفارش در WordPress و gateway reconciliation اختلاف مبلغ نداشته باشد.

### UI، Offline و Resilience

- [ ] `P09-T13` Android موبایل/تبلت و Web عرض ۳۲۰ تا ۱۹۲۰ پیکسل بررسی شوند.
- [ ] `P09-T14` فارسی RTL و انگلیسی LTR در Chrome/Firefox/Edge/Safari بررسی شوند.
- [ ] `P09-T15` keyboard focus، Screen Reader و zoom 200٪ تست شوند.
- [ ] `P09-T16` اینترنت هنگام cart/checkout قطع شود؛ crash یا سفارش ناقص/تکراری ایجاد نشود.
- [ ] `P09-T17` دکمه ثبت سفارش/پرداخت سریع چندبار زده شود؛ فقط یک سفارش/تراکنش ایجاد شود.
- [ ] `P09-T18` Android در میانه checkout Force Stop و دوباره باز شود؛ state امن بازیابی شود.

### Runtime Toggle

- [ ] `P09-T19` قابلیت در wp-admin خاموش شود؛ بدون rebuild از UI حذف شود.
- [ ] `P09-T20` deep link و API مستقیم قابلیت خاموش رد شوند.
- [ ] `P09-T21` قابلیت دوباره روشن شود؛ بدون reinstall و با state صحیح بازگردد.

### Security و Observability

- [ ] `P09-T22` pentest auth،IDOR،CORS،SSRF،payment replay،upload و secret exposure اجرا شود.
- [ ] `P09-T23` بعد از fix هر finding، retest مستقل ثبت شود.
- [ ] `P09-T24` بدون consent هیچ telemetry ارسال نشود.
- [ ] `P09-T25` پس از consent، eventهای واقعی install/login/cart/checkout/payment/error/performance در dashboard دیده شوند.
- [ ] `P09-T26` opt-out ارسال را متوقف و policy حذف/retention را اعمال کند.
- [ ] `P09-T27` payload telemetry فاقد token، موبایل، آدرس، متن درمانی و سایر PII/PHI باشد.

### Closed Beta و عملیات

- [ ] `P09-T28` ابتدا cohort سه تا پنج مشتری/فروشگاه با قرارداد و رضایت معتبر اجرا شود.
- [ ] `P09-T29` defectها، تیکت‌ها، activation، پرداخت و refund cohort ثبت شوند.
- [ ] `P09-T30` پس از رفع top blockerها cohort هشت تا دوازده مشتری اجرا شود.
- [ ] `P09-T31` release drill، incident drill، DB restore و support escalation تمرین شود.
- [ ] `P09-T32` هیچ داده واقعی بیمار یا credential در support bundle/log وجود نداشته باشد.
- [ ] `P09-T33` تمام Criticalها و حداقل ۹۵٪ Majorها PASS باشند و Sev0/Sev1 باز صفر باشد.

### معیار عبور فاز ۰۹

- [ ] نتیجه برای هر SKU/host/backend/artifact جدا ثبت شده است.
- [ ] DONEهای تاریخی جای Evidence جدید را نگرفته‌اند.
- [ ] Gate 018 با تأیید انسانی و بدون شرط باز P0 پاس شده است.

---

## فاز ۱۰ — آمادگی تجاری، Marketplace و انتشار پایدار

### آنچه موجود است

- [x] پیش‌نویس اسناد Theme/Plugin، ماتریس SKU، قیمت‌گذاری، SLA و برنامه بازار در repository وجود دارد.
- [x] Bridge نسخه `1.0.0` اعلام می‌کند.
- [x] Theme فعلاً نسخه `0.8.0` اعلام می‌کند.
- [x] ۱۲ Task تجاری/انسانی هنوز منتظر QA هستند.

### توقف‌گاه‌های انتشار

- [ ] ⛔ ابتدا `P10-BUSINESS-GATE-018` باید PASS شود و سپس `P10-PROGRAM-OPS-019` اجرا شود.
- [ ] ⛔ Git tag واقعی `v1.0.0` ساخته نشده است.
- [ ] ⛔ ZIPهای `carmilla-theme-v1.0.0.zip` و `carmilla-bridge-v1.0.0.zip` وجود ندارند.
- [ ] ⛔ SHA256 و SBOM نهایی وجود ندارند.
- [ ] ⛔ نسخه Theme و Plugin و release manifest یکسان/سازگار نیست.
- [ ] ⛔ workflow فعلی فقط Android debug APK می‌سازد و release کامل نیست.
- [ ] ⛔ `docs/delivery/RELEASE_POLICY.md` ادعاشده وجود ندارد.
- [ ] ⛔ submission ZIP و evidence واقعی demo/RC/partnerها موجود نیست.

### بازبینی تجاری و SKU

- [ ] `P10-T01` ماتریس انتخاب Marketplace با قوانین و درصدهای فعلی ژاکت/راست‌چین دوباره بررسی و تاریخ‌دار شود.
- [ ] `P10-T02` هر SKU دقیقاً فهرست قابلیت، target، host، license، dependency و محدودیت پشتیبانی داشته باشد.
- [ ] `P10-T03` inventory داخل هر ZIP با صفحه فروش همان SKU تطبیق کامل داشته باشد.
- [ ] `P10-T04` هیچ قابلیت بدون Gate در صفحه فروش «Production Ready» نامیده نشود.
- [ ] `P10-T05` قیمت، هزینه متغیر، سهم Marketplace، مالیات، refund، support و break-even با داده واقعی بازبینی شوند.
- [ ] `P10-T06` شرایط کتبی partner/marketplace و مجوز استفاده از نام آن‌ها در Evidence موجود باشد.

### مستندات، ویدیو و Clean-room install

- [ ] `P10-T07` README Theme و Plugin را روی یک ماشین/وردپرس جدید قدم‌به‌قدم اجرا کنید.
- [ ] `P10-T08` کاربر آزمایشی بدون کمک توسعه‌دهنده بتواند نصب، تنظیم و اولین خرید را کامل کند.
- [ ] `P10-T09` ویدیو با داده مصنوعی ضبط شود و هیچ domain، key، موبایل یا اطلاعات واقعی نشان ندهد.
- [ ] `P10-T10` راهنمای upgrade، rollback، backup، troubleshooting و uninstall کامل و صحیح باشد.
- [ ] `P10-T11` Plugin/Theme Check و policyهای Marketplace منتخب روی ZIP نهایی اجرا شوند.

### Demo و Support

- [ ] `P10-T12` URL دمو واقعاً در دسترس، HTTPS و از production/customer data جدا باشد.
- [ ] `P10-T13` reset ساعتی دمو را با ایجاد داده مصنوعی و انتظار یک چرخه کامل اثبات کنید.
- [ ] `P10-T14` ZIP تحویل فاقد source map حساس، credential، log، test artifact و فایل توسعه باشد.
- [ ] `P10-T15` یک تیکت Sev0، یک Sev1 و یک refund را طبق runbook شبیه‌سازی کنید.
- [ ] `P10-T16` زمان پاسخ SLA، escalation owner و ساعت پوشش واقعاً قابل انجام باشند.
- [ ] `P10-T17` kill switch فروش/build را فعال و غیرفعال کنید؛ درخواست جدید متوقف و داده قبلی حفظ شود.

### Marketplace، Pilot و آزمایش قیمت

- [ ] `P10-T18` draft واقعی Marketplace با تصاویر، متن، changelog، license و ZIP صحیح ساخته شود.
- [ ] `P10-T19` preview صفحه فروش با inventory artifact تطبیق و توسط مالک محصول تأیید شود.
- [ ] `P10-T20` limited launch با سقف فروش مصوب اجرا شود.
- [ ] `P10-T21` KPIهای چهار هفته شامل activation، refund، ticket، failure و سود خالص ثبت شوند.
- [ ] `P10-T22` آزمایش A/B قیمت با sample، مدت، معیار توقف و عدم آسیب به خریدار اجرا شود.
- [ ] `P10-T23` compatibility kit با partnerهای دارای رضایت اجرا و نتیجه واقعی ثبت شود.
- [ ] `P10-T24` ورود به Marketplace دوم فقط پس از تحقق شروط پایداری و ظرفیت انجام شود.

### انتشار نسخه پایدار

- [ ] `P10-T25` همه Gateهای prerequisite برای artifact مورد انتشار PASS هستند.
- [ ] `P10-T26` Gate 018 با نام reviewer، تاریخ، نسخه و نتیجه ثبت شده است.
- [ ] `P10-T27` release workflow، Theme/Plugin/PWA و در صورت دامنه Android release را می‌سازد؛ debug artifact منتشر نمی‌شود.
- [ ] `P10-T28` نام و نسخه داخل artifactها با release manifest برابر است.
- [ ] `P10-T29` SHA256 هر artifact و SBOM تولید و دوباره verify شده است.
- [ ] `P10-T30` tag امضاشده/محافظت‌شده `v1.0.0` به commit صحیح اشاره می‌کند.
- [ ] `P10-T31` artifactها در release registry ذخیره و retention مناسب دارند.
- [ ] `P10-T32` post-release smoke روی clean install انجام شده است.
- [ ] `P10-T33` rollback به آخرین RC سالم تمرین و مستند شده است.
- [ ] `P10-T34` تنها پس از تمام موارد بالا `P10-PROGRAM-OPS-019` به DONE معتبر تبدیل شده است.

### معیار عبور فاز ۱۰

- [ ] صفحه فروش، مجوز و inventory هر artifact دقیقاً برابرند.
- [ ] استقلال Theme و Plugin برای همان SKU تست شده است.
- [ ] artifact واقعی، checksum، SBOM، tag و smoke report وجود دارند.
- [ ] هیچ ادعای منتشرنشده درباره Spring، Android Builder، iOS، Desktop، BNPL یا add-onها وجود ندارد.

---

## ۶. ترتیب پیشنهادی اجرای واقعی از امروز

این ترتیب را از بالا به پایین انجام دهید و مرحله‌ای را رد نکنید:

- [ ] `S01` از وضعیت فعلی DB، فایل‌ها و repository backup بگیرید.
- [ ] `S02` خطاهای syntax Theme و Migration را اصلاح و PHP lint را سبز کنید.
- [ ] `S03` نقص‌های P06 در settings/capability/secret/queue را اصلاح کنید.
- [ ] `S04` ZIPهای Theme و Bridge را دوباره بسازید و محتوای Shared Core را کنترل کنید.
- [ ] `S05` Buildهای KMP، Android و PWA را روی commit ثابت بگیرید.
- [ ] `S06` P00 تا P03 را به‌عنوان baseline/security/manifest regression بازآزمایی کنید.
- [ ] `S07` matrix کامل P04 را برای Theme-only، Plugin-only و co-install اجرا کنید.
- [ ] `S08` P05 Core/ZarinPal را اجرا و providerهای فاقد قرارداد را DEFERRED کنید.
- [ ] `S09` P06 messaging را در هر سه host mode اجرا کنید.
- [ ] `S10` P07 Seed Gate A را اجرا؛ Migration Gate B را جدا تست کنید.
- [ ] `S11` PWA را روی HTTPS واقعی و browser/device matrix تست کنید.
- [ ] `S12` P09 regression/security/observability و cohort را کامل کنید.
- [ ] `S13` تناقض وضعیت queue/card/master را فقط بر اساس Evidence واقعی اصلاح کنید.
- [ ] `S14` Gateهای فازها را به ترتیب dependency و با تأیید انسانی ثبت کنید.
- [ ] `S15` فعالیت‌های تجاری P10 و clean-room install را انجام دهید.
- [ ] `S16` Gate 018 را تصویب کنید.
- [ ] `S17` release واقعی، tag، checksum، SBOM و post-release smoke را انجام دهید.

---

## ۷. فرم تأیید نهایی فازها

| فاز | Artifact/Build | تستر | تاریخ | نتیجه | مسیر Evidence | Gate تأیید شد؟ |
|---|---|---|---|---|---|---|
| P00 |  |  |  | `PASS/FAIL` |  | [ ] |
| P01 |  |  |  | `PASS/FAIL` |  | [ ] |
| P02 |  |  |  | `PASS/FAIL` |  | [ ] |
| P03 |  |  |  | `PASS/FAIL` |  | [ ] |
| P04 Theme |  |  |  | `PASS/FAIL` |  | [ ] |
| P04 Plugin |  |  |  | `PASS/FAIL` |  | [ ] |
| P04 Co-install |  |  |  | `PASS/FAIL` |  | [ ] |
| P05 Core/ZarinPal |  |  |  | `PASS/FAIL` |  | [ ] |
| P05 DigiPay |  |  |  | `PASS/FAIL/DEFERRED` |  | [ ] |
| P05 SnappPay |  |  |  | `PASS/FAIL/DEFERRED` |  | [ ] |
| P05 Direct PSP |  |  |  | `PASS/FAIL/DEFERRED` |  | [ ] |
| P06 |  |  |  | `PASS/FAIL` |  | [ ] |
| P07 Seed Gate A |  |  |  | `PASS/FAIL` |  | [ ] |
| P07 Migration Gate B |  |  |  | `PASS/FAIL/DEFERRED` |  | [ ] |
| P08 WordPress PWA |  |  |  | `PASS/FAIL` |  | [ ] |
| P08 Spring PWA |  |  |  | `PASS/FAIL/DEFERRED` |  | [ ] |
| P09 |  |  |  | `PASS/FAIL` |  | [ ] |
| P10 |  |  |  | `PASS/FAIL` |  | [ ] |

---

## ۸. Definition of Done نهایی تا پایان فاز ۱۰

- [ ] همه تست‌های اجباری این سند یا PASS شده‌اند یا با Risk Acceptance رسمی و تاریخ‌دار DEFERRED شده‌اند.
- [ ] هیچ Sev0/Sev1 باز نیست.
- [ ] هیچ Task دستی بدون tester/date/build/result برابر DONE نیست.
- [ ] queue، card، Master و Evidence با هم تناقض ندارند.
- [ ] artifactهای تست‌شده دقیقاً همان artifactهای قابل تحویل‌اند.
- [ ] Theme-only، Plugin-only و co-install برای SKUهای مورد فروش پاس شده‌اند.
- [ ] پرداخت، پیام‌رسانی، Seed/Migration و PWA fail-closed و قابل rollback هستند.
- [ ] هیچ secret، PII یا PHI در source، ZIP، log، analytics و Evidence وجود ندارد.
- [ ] مستندات نصب و پشتیبانی با clean-room test تأیید شده‌اند.
- [ ] Gate تجاری P10 قبل از release پایدار ثبت شده است.
- [ ] tag، ZIP، checksum، SBOM و post-release smoke واقعی و قابل بازتولید هستند.

### تأیید مالک محصول

- نام و امضا: `........................................`
- تاریخ: `........................................`
- Commit/Tag تأییدشده: `........................................`
- نتیجه نهایی: `[ ] PASS   [ ] FAIL   [ ] CONDITIONAL   [ ] DEFERRED`
- توضیحات و Risk Acceptanceها: `........................................`

